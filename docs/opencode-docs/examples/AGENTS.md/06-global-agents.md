# Global AGENTS.md — ~/.config/opencode/AGENTS.md

Applies to every project. Personal preferences, not committed to git.

## Personal Rules
- Always ask before adding new dependencies
- Use `pnpm` over npm or yarn
- Prefer `vitest` over jest for new projects
- Run tests for touched files, not full suite, during development
- Format on save: use project formatter config

## Communication
- Keep responses concise
- Show code diffs for changes
- Explain why when suggesting major refactors
